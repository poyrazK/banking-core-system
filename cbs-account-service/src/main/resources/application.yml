server:
  port: 8083

spring:
  application:
    name: cbs-account-service
  datasource:
    url: jdbc:postgresql://localhost:5432/cbs_account
    username: cbs
    password: cbs
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
